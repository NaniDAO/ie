import { Shell } from "@/components/shell";
import { Header } from "@/components/header";

const Home = () => {
  return (
    <main className="p-1">
      <Header />
      <Shell />
    </main>
  );
};

export default Home;
